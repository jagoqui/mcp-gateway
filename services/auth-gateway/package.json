{
  "name": "auth-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Single-perimeter authentication gateway for mcp-gateway: token/session verification consumed by Caddy forward_auth.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --check ."
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "bcrypt": "^6.0.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^26.3.0",
    "eslint": "^10.9.1",
    "prettier": "^3.9.6",
    "typescript": "^7.0.2"
  }
}
